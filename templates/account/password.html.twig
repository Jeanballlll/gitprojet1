{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe - Projet 1{% endblock %}

{% block content %}
    <h1>Modifier mot de passe</h1>
    <a href="{{ path('app_account') }}">Retourner à mon compte</a><br>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}
